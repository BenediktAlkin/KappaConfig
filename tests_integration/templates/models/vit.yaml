vars:
  patch_size: 16

debug:
  patch_size: ${vars.patch_size}
  embedding_dim: 16
  depth: 3
  attention_heads: 2
  mlp_ratio: 1

base:
  patch_size: ${vars.patch_size}
  embedding_dim: 768
  depth: 12
  attention_heads: 12
  mlp_ratio: 4